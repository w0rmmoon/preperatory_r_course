{"title":"Preperatory R Course - Topic Two","markdown":{"yaml":{"title":"Preperatory R Course - Topic Two","format":"html","editor":"visual"},"headingText":"Vectors","containsRefs":false,"markdown":"\n\nWelcome back to lesson two! In this lesson we will cover vectors (a one-dimensional data format), logical expressions and how to handle missing data.\n\n\nIn lession one, we learned about simple data types (numeric, character, ...). For example, if you measured the DBH of a tree, you may assign the numeric object to a variable DBH:\n\n```{r}\ndbh <- 8.42\n```\n\nHowever, often, you want to save multiple numerical objects in the same variable (e.g. the measured height of many different trees). To do this, you use the function `c()` to create a **vector**. The c stands for concatenate. For example:\n\n```{r}\ndbh_vector <- c(8.43, 16.22, 14.49, 19.28, 10.13, 7.23)\n```\n\nA vector holds **any number of ordered objects of the same data type**. You can use `str()` to see the data type and the length (number of objects) of a vector. You can also use `length()` to see the length of a vector.\n\n```{r}\nstr(dbh_vector) # numeric vector with 6 elements\nlength(dbh_vector)\n```\n\nIf the objects are not of the same type, R will automatically try to convert them to the same object (if this is not possible, there will be an error). Consider the following vector where all values are converted to the character data type:\n\n```{r}\n(my_vector <- c(10, \"abcdefg\", 9.2, TRUE))\nstr(my_vector)\n```\n\n::: callout-tip\nIf you add brackets around an assignment the object is automatically printed to the console!\n:::\n\n## Generating Vectors\n\nYou can generate vectors that contain ordered numbers (e.g. numbers 10 through 20) like this:\n\n```{r}\n(my_vector <- 1:20)\n```\n\nIf you want to have a different step size, you can use the function `seq().`\n\n```{r}\n(my_vector <- seq(from = 1, to = 10, by = 0.5)) # vector from 1 to 10 with a step size of 0.5\n\n(my_vector_2 <- seq(from = 5, to = 10, length.out = 7)) # vector with 7 equally spaced numbers between 5 and 10\n```\n\nAlternatively, you can use the function `rep()` to create a repeating vector.\n\n```{r}\nrep(7, times = 10) # repeat the number 7 ten times\n\n# Note: You can give a vector as an argument to rep() to repeat the vector\nrep(c(1, 2, 3), times = 10)\n\n# Use \"each\" instead of \"times\" to order a vector as 1, 1, 1, 2, 2, 2... instead of 1, 2, 1, 2...\nrep(c(1, 2, 3), each = 10)\n\n# You can use rep() with non-numeric values\n(detention <- rep(\"I must not tell lies.\", times = 10))\n```\n\n## Vector Calculations\n\nYou can calculate with numeric vectors using the same arithmetic expressions from the first lesson. For example:\n\n```{r}\n# add 10 to every element of the vector\nmy_vector <- 1:5\nmy_vector + 10\n```\n\nYou can also do calculations with two vectors of the same length.\n\n```{r}\n# Calculating volume of a cylinder\nheight <- c(108, 120, 138, 119)\nradius <- c(14, 19, 28, 30)\n\n# Note: pi is a built-in constant in R so we do not need\n# to define it.\n(volume <- pi * radius^2 * height)\n```\n\nIf vectors have different lengths, the shorter vector will be *recycled*. This means it will be repeated to match the length of the longer vector; for example, in this case, `my_vector_2` is treated as `c(1, 2, 3, 1)`.\n\n```{r, error = T}\nmy_vector_1 <- c(1, 2, 3, 4)\nmy_vector_2 <- c(1, 2, 3)\n\nmy_vector_1 - my_vector_2 # raises a warning, but calculation proceeds\n```\n\n## Selecting Vector Elements\n\nTo select an element of a vector, use square brackets with the number of the element you want to select.\n\n```{r}\nbody_size <- c(10.4, 12.3, 8.1, 14.8)\nbody_size[2] # select the second element of body_size\n```\n\nYou can select multiple elements by using a vector within the brackets:\n\n```{r}\nbody_size[1:3]\nbody_size[c(2, 4)]\n```\n\nOr you can select all elements except specific ones:\n\n```{r}\nbody_size[-1]\n```\n\nYou can also replace the selected values.\\\n\n```{r}\nbody_size[1] <- 1000\nbody_size\n```\n\n## Logical Expressions & Operators\n\n### Logical Expressions\n\nA logical expression is an **expression that returns either TRUE or FALSE** (an object with the logical data type). Common logical expressions are the comparison expressions:\n\n```{r}\n4 > 10 # Greater than\n10 >= 10 # Greater than or equal to\n18 < 29 # Smaller than\n12 < 7 # Smaller than or equal to\n5 == 2 # Equals\n5 != 3 # Does not equal\n```\n\nYou can also use these operators with characters. Greater/Smaller Than then refers to alphabetical order.\n\n```{r}\n\"cat\" == \"dog\"\n\"cat\" > \"dog\" # cat is earlier in the alphabet and therefore \"smaller\"\n```\n\nYou can also use logical expressions with vectors. In these cases, the same rules as vector calculations apply:\n\n```{r}\nmy_vector <- c(1, 2, 3, 4, 5)\nmy_vector > 3 # comparison to a single value \nmy_vector == c(5, 4, 3, 2, 1) # comparison to another vector\n```\n\nLogical expressions with vectors always return a **logical vector** (vector with only TRUE/FALSE values). You can also use a TRUE/FALSE vector to select values from a vector:\n\n```{r}\n# select only value 1 and 5\nselection_vector <- c(TRUE, FALSE, FALSE, FALSE, TRUE)\nmy_vector[selection_vector]\n```\n\nThis is useful because it allows you to select values from a vector based on logical conditions. For example, if you are only interested in trees above a certain height:\n\n```{r}\ntree_heights <- c(10, 12, 7, 9, 11, 13)\n\n# only select trees > 10 m height\n# note that I have to write tree_heights twice\n(tall_trees <- tree_heights[tree_heights >= 10])\n```\n\nYou can also use the **%in% operator** with vectors. The %in% operator tells you whether the object on the left is in the object on the right:\n\n```{r}\n5 %in% c(1, 2, 3, 4, 5)\n\n# with a vector on the left, each vector element\n# is compared to the right\nc(1, 2, 3, 4, 5) %in% 5\n\n# you can also compare two vectors\nc(3, 9, 2, 4, 8) %in% c(1, 2, 3, 4)\n```\n\n### Logical Operators\n\nLogical operators **combine** multiple logical expressions. Logical operators recognized by R are AND (`&`), OR (`|`) and NOT (`!`).\n\n```{r}\n# AND\n# For this expression to be TRUE,\n# both sides need to be true\n10 > 1 & 10 > 100\n\n# OR\n# For this expression to be TRUE,\n# only one side needs to be TRUE\n10 > 1 | 10 > 100\n\n# NOT\n# Not negates the value\n# of the expression\n!(\"cat\" == \"cat\")\n\n# same as:\n\"cat\" != \"cat\"\n```\n\nYou can use logical operators to select data with more criteria:\n\n```{r}\ntree_age <- c(2, 10, 9, 19, 21, 17, 4, 29)\n\n# select only trees between age 10 and 20\ntree_age[tree_age >= 10 & tree_age <= 20]\n```\n\n::: panel-tabset\n## Comprehension Question\n\nWhat values are selected in this expression?\n\n`vector <- 1:20`\n\n`vector[vector < 5 | vector > 15]`\n\n## Answer\n\n1, 2, 3, 4, 16, 17, 18, 19, 20\n:::\n\n## Functions and Vectors\n\nYou can use vectors as an argument in functions. You can use most functions you can use to manipulate single values (e.g. `round()` or `sqrt()`) with vectors as well.\n\n```{r}\ntree_dbh <- c(20.01, 18.73, 29.28, 14.93)\nround(tree_dbh, digits = 1)\n```\n\nThere are multiple functions that provide summary statistics of a vector:\n\n```{r}\nmean(tree_dbh) # mean\nmedian(tree_dbh) # median\nsd(tree_dbh) # standard deviation\nmax(tree_dbh) # maximum\nwhich.max(tree_dbh) # which element is the maximum element?\nmin(tree_dbh) # minimum\nrange(dbh)\n\n```\n\nYou can also use the function `sort()` to sort a vector:\n\n```{r}\nsort(tree_dbh)\nsort(tree_dbh, decreasing = TRUE)\n```\n\nAnd you can use the function `any()` and `all()` with logical vectors to determine whether any/all values are TRUE.\n\n```{r}\n# Are there any DBHs over 20 cm?\nany(tree_dbh > 20)\n\n# Are all DBHs over 20 cm?\nall(tree_dbh > 20)\n```\n\n## Missing Values / NA\n\nReal data often contains missing values. For example, if you measured the temperature over the course of 5 days but had an instrument error on the 4th day, you may want to indicate this by setting the value as \"missing\". For this, you use `NA` (\"Not Available\").\n\n```{r}\ntemperature <- c(10.3, 11.9, 9.3, NA, 12.1)\n```\n\nUsing NA in calculations or logical expressions always results in NA.\n\n```{r}\n4 * NA\n```\n\nMany functions therefore don't work if the data includes missing values, but most of these functions take an optional argument that determines what to do with missing values. For example, the function `sum()` calculates the sum of all values in a vector and only works if the argument `na.rm` is set to TRUE.\n\n```{r}\n# does not work\nsum(temperature)\n\n# removes NAs before summing and therefore works\nsum(temperature, na.rm = TRUE)\n```\n\nSince you can't use logical expressions to check for NAs, you can't use them to determine whether your data contains NA:\n\n```{r}\nNA == temperature\n```\n\nInstead, use the function `is.na()`. In combination with the `any()` function, you can see at a quick glance whether there are any missing values in your data.\n\n```{r}\n# see which values are NA\nis.na(temperature)\n\n# see if any values are NA\nany(is.na(temperature))\n```\n\n::: {.callout-note collapse=\"true\"}\n## Advanced: Lists\n\nNEEDS TO BE WRITTEN STILL\n:::\n","srcMarkdownNoYaml":"\n\nWelcome back to lesson two! In this lesson we will cover vectors (a one-dimensional data format), logical expressions and how to handle missing data.\n\n# Vectors\n\nIn lession one, we learned about simple data types (numeric, character, ...). For example, if you measured the DBH of a tree, you may assign the numeric object to a variable DBH:\n\n```{r}\ndbh <- 8.42\n```\n\nHowever, often, you want to save multiple numerical objects in the same variable (e.g. the measured height of many different trees). To do this, you use the function `c()` to create a **vector**. The c stands for concatenate. For example:\n\n```{r}\ndbh_vector <- c(8.43, 16.22, 14.49, 19.28, 10.13, 7.23)\n```\n\nA vector holds **any number of ordered objects of the same data type**. You can use `str()` to see the data type and the length (number of objects) of a vector. You can also use `length()` to see the length of a vector.\n\n```{r}\nstr(dbh_vector) # numeric vector with 6 elements\nlength(dbh_vector)\n```\n\nIf the objects are not of the same type, R will automatically try to convert them to the same object (if this is not possible, there will be an error). Consider the following vector where all values are converted to the character data type:\n\n```{r}\n(my_vector <- c(10, \"abcdefg\", 9.2, TRUE))\nstr(my_vector)\n```\n\n::: callout-tip\nIf you add brackets around an assignment the object is automatically printed to the console!\n:::\n\n## Generating Vectors\n\nYou can generate vectors that contain ordered numbers (e.g. numbers 10 through 20) like this:\n\n```{r}\n(my_vector <- 1:20)\n```\n\nIf you want to have a different step size, you can use the function `seq().`\n\n```{r}\n(my_vector <- seq(from = 1, to = 10, by = 0.5)) # vector from 1 to 10 with a step size of 0.5\n\n(my_vector_2 <- seq(from = 5, to = 10, length.out = 7)) # vector with 7 equally spaced numbers between 5 and 10\n```\n\nAlternatively, you can use the function `rep()` to create a repeating vector.\n\n```{r}\nrep(7, times = 10) # repeat the number 7 ten times\n\n# Note: You can give a vector as an argument to rep() to repeat the vector\nrep(c(1, 2, 3), times = 10)\n\n# Use \"each\" instead of \"times\" to order a vector as 1, 1, 1, 2, 2, 2... instead of 1, 2, 1, 2...\nrep(c(1, 2, 3), each = 10)\n\n# You can use rep() with non-numeric values\n(detention <- rep(\"I must not tell lies.\", times = 10))\n```\n\n## Vector Calculations\n\nYou can calculate with numeric vectors using the same arithmetic expressions from the first lesson. For example:\n\n```{r}\n# add 10 to every element of the vector\nmy_vector <- 1:5\nmy_vector + 10\n```\n\nYou can also do calculations with two vectors of the same length.\n\n```{r}\n# Calculating volume of a cylinder\nheight <- c(108, 120, 138, 119)\nradius <- c(14, 19, 28, 30)\n\n# Note: pi is a built-in constant in R so we do not need\n# to define it.\n(volume <- pi * radius^2 * height)\n```\n\nIf vectors have different lengths, the shorter vector will be *recycled*. This means it will be repeated to match the length of the longer vector; for example, in this case, `my_vector_2` is treated as `c(1, 2, 3, 1)`.\n\n```{r, error = T}\nmy_vector_1 <- c(1, 2, 3, 4)\nmy_vector_2 <- c(1, 2, 3)\n\nmy_vector_1 - my_vector_2 # raises a warning, but calculation proceeds\n```\n\n## Selecting Vector Elements\n\nTo select an element of a vector, use square brackets with the number of the element you want to select.\n\n```{r}\nbody_size <- c(10.4, 12.3, 8.1, 14.8)\nbody_size[2] # select the second element of body_size\n```\n\nYou can select multiple elements by using a vector within the brackets:\n\n```{r}\nbody_size[1:3]\nbody_size[c(2, 4)]\n```\n\nOr you can select all elements except specific ones:\n\n```{r}\nbody_size[-1]\n```\n\nYou can also replace the selected values.\\\n\n```{r}\nbody_size[1] <- 1000\nbody_size\n```\n\n## Logical Expressions & Operators\n\n### Logical Expressions\n\nA logical expression is an **expression that returns either TRUE or FALSE** (an object with the logical data type). Common logical expressions are the comparison expressions:\n\n```{r}\n4 > 10 # Greater than\n10 >= 10 # Greater than or equal to\n18 < 29 # Smaller than\n12 < 7 # Smaller than or equal to\n5 == 2 # Equals\n5 != 3 # Does not equal\n```\n\nYou can also use these operators with characters. Greater/Smaller Than then refers to alphabetical order.\n\n```{r}\n\"cat\" == \"dog\"\n\"cat\" > \"dog\" # cat is earlier in the alphabet and therefore \"smaller\"\n```\n\nYou can also use logical expressions with vectors. In these cases, the same rules as vector calculations apply:\n\n```{r}\nmy_vector <- c(1, 2, 3, 4, 5)\nmy_vector > 3 # comparison to a single value \nmy_vector == c(5, 4, 3, 2, 1) # comparison to another vector\n```\n\nLogical expressions with vectors always return a **logical vector** (vector with only TRUE/FALSE values). You can also use a TRUE/FALSE vector to select values from a vector:\n\n```{r}\n# select only value 1 and 5\nselection_vector <- c(TRUE, FALSE, FALSE, FALSE, TRUE)\nmy_vector[selection_vector]\n```\n\nThis is useful because it allows you to select values from a vector based on logical conditions. For example, if you are only interested in trees above a certain height:\n\n```{r}\ntree_heights <- c(10, 12, 7, 9, 11, 13)\n\n# only select trees > 10 m height\n# note that I have to write tree_heights twice\n(tall_trees <- tree_heights[tree_heights >= 10])\n```\n\nYou can also use the **%in% operator** with vectors. The %in% operator tells you whether the object on the left is in the object on the right:\n\n```{r}\n5 %in% c(1, 2, 3, 4, 5)\n\n# with a vector on the left, each vector element\n# is compared to the right\nc(1, 2, 3, 4, 5) %in% 5\n\n# you can also compare two vectors\nc(3, 9, 2, 4, 8) %in% c(1, 2, 3, 4)\n```\n\n### Logical Operators\n\nLogical operators **combine** multiple logical expressions. Logical operators recognized by R are AND (`&`), OR (`|`) and NOT (`!`).\n\n```{r}\n# AND\n# For this expression to be TRUE,\n# both sides need to be true\n10 > 1 & 10 > 100\n\n# OR\n# For this expression to be TRUE,\n# only one side needs to be TRUE\n10 > 1 | 10 > 100\n\n# NOT\n# Not negates the value\n# of the expression\n!(\"cat\" == \"cat\")\n\n# same as:\n\"cat\" != \"cat\"\n```\n\nYou can use logical operators to select data with more criteria:\n\n```{r}\ntree_age <- c(2, 10, 9, 19, 21, 17, 4, 29)\n\n# select only trees between age 10 and 20\ntree_age[tree_age >= 10 & tree_age <= 20]\n```\n\n::: panel-tabset\n## Comprehension Question\n\nWhat values are selected in this expression?\n\n`vector <- 1:20`\n\n`vector[vector < 5 | vector > 15]`\n\n## Answer\n\n1, 2, 3, 4, 16, 17, 18, 19, 20\n:::\n\n## Functions and Vectors\n\nYou can use vectors as an argument in functions. You can use most functions you can use to manipulate single values (e.g. `round()` or `sqrt()`) with vectors as well.\n\n```{r}\ntree_dbh <- c(20.01, 18.73, 29.28, 14.93)\nround(tree_dbh, digits = 1)\n```\n\nThere are multiple functions that provide summary statistics of a vector:\n\n```{r}\nmean(tree_dbh) # mean\nmedian(tree_dbh) # median\nsd(tree_dbh) # standard deviation\nmax(tree_dbh) # maximum\nwhich.max(tree_dbh) # which element is the maximum element?\nmin(tree_dbh) # minimum\nrange(dbh)\n\n```\n\nYou can also use the function `sort()` to sort a vector:\n\n```{r}\nsort(tree_dbh)\nsort(tree_dbh, decreasing = TRUE)\n```\n\nAnd you can use the function `any()` and `all()` with logical vectors to determine whether any/all values are TRUE.\n\n```{r}\n# Are there any DBHs over 20 cm?\nany(tree_dbh > 20)\n\n# Are all DBHs over 20 cm?\nall(tree_dbh > 20)\n```\n\n## Missing Values / NA\n\nReal data often contains missing values. For example, if you measured the temperature over the course of 5 days but had an instrument error on the 4th day, you may want to indicate this by setting the value as \"missing\". For this, you use `NA` (\"Not Available\").\n\n```{r}\ntemperature <- c(10.3, 11.9, 9.3, NA, 12.1)\n```\n\nUsing NA in calculations or logical expressions always results in NA.\n\n```{r}\n4 * NA\n```\n\nMany functions therefore don't work if the data includes missing values, but most of these functions take an optional argument that determines what to do with missing values. For example, the function `sum()` calculates the sum of all values in a vector and only works if the argument `na.rm` is set to TRUE.\n\n```{r}\n# does not work\nsum(temperature)\n\n# removes NAs before summing and therefore works\nsum(temperature, na.rm = TRUE)\n```\n\nSince you can't use logical expressions to check for NAs, you can't use them to determine whether your data contains NA:\n\n```{r}\nNA == temperature\n```\n\nInstead, use the function `is.na()`. In combination with the `any()` function, you can see at a quick glance whether there are any missing values in your data.\n\n```{r}\n# see which values are NA\nis.na(temperature)\n\n# see if any values are NA\nany(is.na(temperature))\n```\n\n::: {.callout-note collapse=\"true\"}\n## Advanced: Lists\n\nNEEDS TO BE WRITTEN STILL\n:::\n"},"formats":{"html":{"identifier":{"display-name":"HTML","target-format":"html","base-format":"html"},"execute":{"fig-width":7,"fig-height":5,"fig-format":"retina","fig-dpi":96,"df-print":"default","error":false,"eval":true,"cache":null,"freeze":false,"echo":true,"output":true,"warning":true,"include":true,"keep-md":false,"keep-ipynb":false,"ipynb":null,"enabled":null,"daemon":null,"daemon-restart":false,"debug":false,"ipynb-filters":[],"ipynb-shell-interactivity":null,"plotly-connected":true,"engine":"knitr"},"render":{"keep-tex":false,"keep-typ":false,"keep-source":false,"keep-hidden":false,"prefer-html":false,"output-divs":true,"output-ext":"html","fig-align":"default","fig-pos":null,"fig-env":null,"code-fold":"none","code-overflow":"scroll","code-link":false,"code-line-numbers":false,"code-tools":false,"tbl-colwidths":"auto","merge-includes":true,"inline-includes":false,"preserve-yaml":false,"latex-auto-mk":true,"latex-auto-install":true,"latex-clean":true,"latex-min-runs":1,"latex-max-runs":10,"latex-makeindex":"makeindex","latex-makeindex-opts":[],"latex-tlmgr-opts":[],"latex-input-paths":[],"latex-output-dir":null,"link-external-icon":false,"link-external-newwindow":false,"self-contained-math":false,"format-resources":[],"notebook-links":true},"pandoc":{"standalone":true,"wrap":"none","default-image-extension":"png","to":"html","toc":true,"output-file":"02_Vectors.html"},"language":{"toc-title-document":"Table of contents","toc-title-website":"On this page","related-formats-title":"Other Formats","related-notebooks-title":"Notebooks","source-notebooks-prefix":"Source","other-links-title":"Other Links","code-links-title":"Code Links","launch-dev-container-title":"Launch Dev Container","launch-binder-title":"Launch Binder","article-notebook-label":"Article Notebook","notebook-preview-download":"Download Notebook","notebook-preview-download-src":"Download Source","notebook-preview-back":"Back to Article","manuscript-meca-bundle":"MECA Bundle","section-title-abstract":"Abstract","section-title-appendices":"Appendices","section-title-footnotes":"Footnotes","section-title-references":"References","section-title-reuse":"Reuse","section-title-copyright":"Copyright","section-title-citation":"Citation","appendix-attribution-cite-as":"For attribution, please cite this work as:","appendix-attribution-bibtex":"BibTeX citation:","appendix-view-license":"View License","title-block-author-single":"Author","title-block-author-plural":"Authors","title-block-affiliation-single":"Affiliation","title-block-affiliation-plural":"Affiliations","title-block-published":"Published","title-block-modified":"Modified","title-block-keywords":"Keywords","callout-tip-title":"Tip","callout-note-title":"Note","callout-warning-title":"Warning","callout-important-title":"Important","callout-caution-title":"Caution","code-summary":"Code","code-tools-menu-caption":"Code","code-tools-show-all-code":"Show All Code","code-tools-hide-all-code":"Hide All Code","code-tools-view-source":"View Source","code-tools-source-code":"Source Code","tools-share":"Share","tools-download":"Download","code-line":"Line","code-lines":"Lines","copy-button-tooltip":"Copy to Clipboard","copy-button-tooltip-success":"Copied!","repo-action-links-edit":"Edit this page","repo-action-links-source":"View source","repo-action-links-issue":"Report an issue","back-to-top":"Back to top","search-no-results-text":"No results","search-matching-documents-text":"matching documents","search-copy-link-title":"Copy link to search","search-hide-matches-text":"Hide additional matches","search-more-match-text":"more match in this document","search-more-matches-text":"more matches in this document","search-clear-button-title":"Clear","search-text-placeholder":"","search-detached-cancel-button-title":"Cancel","search-submit-button-title":"Submit","search-label":"Search","toggle-section":"Toggle section","toggle-sidebar":"Toggle sidebar navigation","toggle-dark-mode":"Toggle dark mode","toggle-reader-mode":"Toggle reader mode","toggle-navigation":"Toggle navigation","crossref-fig-title":"Figure","crossref-tbl-title":"Table","crossref-lst-title":"Listing","crossref-thm-title":"Theorem","crossref-lem-title":"Lemma","crossref-cor-title":"Corollary","crossref-prp-title":"Proposition","crossref-cnj-title":"Conjecture","crossref-def-title":"Definition","crossref-exm-title":"Example","crossref-exr-title":"Exercise","crossref-ch-prefix":"Chapter","crossref-apx-prefix":"Appendix","crossref-sec-prefix":"Section","crossref-eq-prefix":"Equation","crossref-lof-title":"List of Figures","crossref-lot-title":"List of Tables","crossref-lol-title":"List of Listings","environment-proof-title":"Proof","environment-remark-title":"Remark","environment-solution-title":"Solution","listing-page-order-by":"Order By","listing-page-order-by-default":"Default","listing-page-order-by-date-asc":"Oldest","listing-page-order-by-date-desc":"Newest","listing-page-order-by-number-desc":"High to Low","listing-page-order-by-number-asc":"Low to High","listing-page-field-date":"Date","listing-page-field-title":"Title","listing-page-field-description":"Description","listing-page-field-author":"Author","listing-page-field-filename":"File Name","listing-page-field-filemodified":"Modified","listing-page-field-subtitle":"Subtitle","listing-page-field-readingtime":"Reading Time","listing-page-field-wordcount":"Word Count","listing-page-field-categories":"Categories","listing-page-minutes-compact":"{0} min","listing-page-category-all":"All","listing-page-no-matches":"No matching items","listing-page-words":"{0} words","listing-page-filter":"Filter","draft":"Draft"},"metadata":{"lang":"en","fig-responsive":true,"quarto-version":"1.9.38","engines":[{"path":"C:\\PROGRA~1\\RStudio\\resources\\app\\bin\\quarto\\share\\extension-subtrees\\julia-engine\\_extensions\\julia-engine\\julia-engine.js"}],"theme":"cosmo","title":"Preperatory R Course - Topic Two","editor":"visual"},"extensions":{"book":{"multiFile":true}}}},"projectFormats":["html"]}