{"title":"Preperatory R Course - Topic 3","markdown":{"yaml":{"title":"Preperatory R Course - Topic 3","format":"html","editor":"visual"},"headingText":"Data Frames","containsRefs":false,"markdown":"\n\nWelcome to topic 3! In this lesson, we will cover data frames and matrices (2-dimensional data structures).\n\n\nYou are already familiar with vectors (1-dimensional data structures). You may already have realized an issue with vectors: We can only save one variable per vector. If we measure multiple variables, for example the diameter at breast height (DBH), height, and species of a tree, we might instead want to record this information in a table with rows and columns. This is where **2-dimensional data structures** like data frames become relevant.\n\nA data frame consists of rows and columns (similar to for example an excel table). Each column holds one vector: this means that every column can hold a different data type, but within a column the data type needs to be consistent. It is therefore good practice to have **one row per observation** (for example, one row per measured tree) and **one column per measured variable** (so, for example, one column for DBH, height and species respectively). The columns in a data frame are usually named; the rows are typically not (but can be named if necessary).\n\nTo create a data frame, use the `data.frame()` function.\n\n```{r}\n# creates a dataframe with 3 columns and 4 rows\ntrees <- data.frame(\n  dbh = c(29, 16, 18, 21),\n  height = c(10, 12, 23, 17),\n  species = c(\"Beech\", \"Beech\", \"Fir\", \"Fir\")\n)\n\n# you can use str() for a quick overview\n# of a data frame\nstr(trees)\n```\n\nA data frame needs to have the same number of rows in every column:\n\n```{r, error  = TRUE}\n\n# Raises an error since the number of rows does not match\ndata.frame(\n  var1 = c(1, 2, 3, 4),\n  var2 = c(\"Blue\", \"Green\", \"Yellow\")\n)\n```\n\n::: callout-caution\nIf the number of rows of one column is a multiple of the number of rows of the other (for example, 2 and 4 or 30 and 90 rows), the shorter column will be recycled.\n:::\n\n## Selecting Data\n\nTo select data from a data frame, you can also use \\[square brackets\\]. However, since the data has two dimensions, you need to include both the row and the column number of the data you want to access. For this, remember **RoCo: Rows First, Columns Second.**\n\n```{r}\ntrees[1, 3] # select the first row, third column\n\ntrees [2:3, c(1, 3)] # select row 2 and 3, column 1 and 3\n```\n\nIf you want to select all rows/columns, you still need to add the comma, but not add a number.\n\n```{r}\ntrees[, 3] # select all entries in the third column\n```\n\nIf you columns have names, you can also **select columns by name.**\n\n```{r}\ntrees[1, \"height\"] # first entry in the height column\n\ntrees[, c(\"height\", \"dbh\")] # height & dbh column\n```\n\nIf you only want to select a single column by name, you can use the \\$ operator instead.\n\n```{r}\ntrees$height # select the entire height column\n\ntrees$dbh[2] # select the second row of the dbh column\n```\n\nJust like with vectors, you can use logical expressions and operators to select data.\n\n```{r}\n# select trees with height > 20 m\ntrees[trees$height > 20, ]\n\n# select trees with dbh < 20 and of species fir\ntrees[trees$dbh < 20 & trees$species == \"Fir\", ]\n```\n\nAnd you can replace selected values:\n\n```{r}\ntrees[1, 3] <- \"Pine\"\n```\n\n## Summary Statistics\n\nAs an example for this lesson, we will use one of R's many **built-in datasets** (we will cover how to read in your own data instead soon). These datasets are useful for instruction and practicing your coding. To get an overview of all available data sets, run:\n\n```{r}\nlibrary(help = \"datasets\")\n```\n\nIn this case, we will use the `iris` data. This data contains measurements of 150 flowers of different iris species. For a more detailed description of the dataset, you can run `?iris`. To load the data, use:\n\n```{r}\ndata(iris)\n```\n\nTo gain a first overview of the data, you can use the `str()` command. Then use the `summary()` command for more detailed information, including the most common summary statistics (mean, maximum, minimum, quantiles).\n\n```{r}\nstr(iris)\n\nsummary(iris)\n```\n\n## Factors\n\nIf we call `summary()` on our trees dataframe we can see that the information in the species column is not particularly helpful - we can see that there are four character objects, but not what they contain or how many trees of which species we have (like with the iris dataset above). This is because R does currently not recognize that these characters represent **categorical data.**\n\n```{r}\nsummary(trees)\n```\n\nThe categorical data type in R is called a **factor**. You can easily convert a variable into a factor using the `factor()` function.\n\n```{r}\ntrees$species_factor <- factor(trees$species)\n\n# Now the species_factor column provides a more useful summary\nsummary(trees)\n```\n\nEach factor has **levels** (categories). You can use the `levels()` function or `str()` to inspect them.\n\n```{r}\nlevels(trees$species_factor)\nstr(trees)\n```\n\nInternally, a factor saves a number for each observation. The number represents the category; in this case, 1 represents \"Beech\" and 2 represents \"Fir\".\n\nWhen converting a variable into a factor, you can optionally use the arguments `levels` and `labels`. By specifying \"levels\", you can change the order of your factor levels (otherwise, levels will be ordered alphabetically by default). With \"labels\", you can change the name of each factor level that will displayed in the summary statistics. Specifying levels and labels becomes especially relevant when plotting categorical data (covered in lesson 5).\n\nFor example, if you want to change the order of your level so that \"Fir\" is the first level and change the display names to their scientific names, you would use this code:\n\n```{r}\n# You can split up function arguments into multiple lines for readability\ntrees$species_factor <- factor(\n  trees$species,\n  levels = c(\"Fir\", \"Beech\"),\n  labels = c(\"Abies alba\", \"Fagus sylvatica\")\n  )\nsummary(trees)\n```\n\n## Renaming Columns\n\nYou can use the `names()` function to access the column names of a data frame.\n\n```{r}\ncolnames(trees)\n```\n\nYou can also use this function to rename the columns using an assignment:\n\n```{r}\ncolnames(trees) <- c(\"dbh_cm\", \"height_m\", \"species_chr\", \"species_factor\")\nstr(trees)\n```\n","srcMarkdownNoYaml":"\n\nWelcome to topic 3! In this lesson, we will cover data frames and matrices (2-dimensional data structures).\n\n## Data Frames\n\nYou are already familiar with vectors (1-dimensional data structures). You may already have realized an issue with vectors: We can only save one variable per vector. If we measure multiple variables, for example the diameter at breast height (DBH), height, and species of a tree, we might instead want to record this information in a table with rows and columns. This is where **2-dimensional data structures** like data frames become relevant.\n\nA data frame consists of rows and columns (similar to for example an excel table). Each column holds one vector: this means that every column can hold a different data type, but within a column the data type needs to be consistent. It is therefore good practice to have **one row per observation** (for example, one row per measured tree) and **one column per measured variable** (so, for example, one column for DBH, height and species respectively). The columns in a data frame are usually named; the rows are typically not (but can be named if necessary).\n\nTo create a data frame, use the `data.frame()` function.\n\n```{r}\n# creates a dataframe with 3 columns and 4 rows\ntrees <- data.frame(\n  dbh = c(29, 16, 18, 21),\n  height = c(10, 12, 23, 17),\n  species = c(\"Beech\", \"Beech\", \"Fir\", \"Fir\")\n)\n\n# you can use str() for a quick overview\n# of a data frame\nstr(trees)\n```\n\nA data frame needs to have the same number of rows in every column:\n\n```{r, error  = TRUE}\n\n# Raises an error since the number of rows does not match\ndata.frame(\n  var1 = c(1, 2, 3, 4),\n  var2 = c(\"Blue\", \"Green\", \"Yellow\")\n)\n```\n\n::: callout-caution\nIf the number of rows of one column is a multiple of the number of rows of the other (for example, 2 and 4 or 30 and 90 rows), the shorter column will be recycled.\n:::\n\n## Selecting Data\n\nTo select data from a data frame, you can also use \\[square brackets\\]. However, since the data has two dimensions, you need to include both the row and the column number of the data you want to access. For this, remember **RoCo: Rows First, Columns Second.**\n\n```{r}\ntrees[1, 3] # select the first row, third column\n\ntrees [2:3, c(1, 3)] # select row 2 and 3, column 1 and 3\n```\n\nIf you want to select all rows/columns, you still need to add the comma, but not add a number.\n\n```{r}\ntrees[, 3] # select all entries in the third column\n```\n\nIf you columns have names, you can also **select columns by name.**\n\n```{r}\ntrees[1, \"height\"] # first entry in the height column\n\ntrees[, c(\"height\", \"dbh\")] # height & dbh column\n```\n\nIf you only want to select a single column by name, you can use the \\$ operator instead.\n\n```{r}\ntrees$height # select the entire height column\n\ntrees$dbh[2] # select the second row of the dbh column\n```\n\nJust like with vectors, you can use logical expressions and operators to select data.\n\n```{r}\n# select trees with height > 20 m\ntrees[trees$height > 20, ]\n\n# select trees with dbh < 20 and of species fir\ntrees[trees$dbh < 20 & trees$species == \"Fir\", ]\n```\n\nAnd you can replace selected values:\n\n```{r}\ntrees[1, 3] <- \"Pine\"\n```\n\n## Summary Statistics\n\nAs an example for this lesson, we will use one of R's many **built-in datasets** (we will cover how to read in your own data instead soon). These datasets are useful for instruction and practicing your coding. To get an overview of all available data sets, run:\n\n```{r}\nlibrary(help = \"datasets\")\n```\n\nIn this case, we will use the `iris` data. This data contains measurements of 150 flowers of different iris species. For a more detailed description of the dataset, you can run `?iris`. To load the data, use:\n\n```{r}\ndata(iris)\n```\n\nTo gain a first overview of the data, you can use the `str()` command. Then use the `summary()` command for more detailed information, including the most common summary statistics (mean, maximum, minimum, quantiles).\n\n```{r}\nstr(iris)\n\nsummary(iris)\n```\n\n## Factors\n\nIf we call `summary()` on our trees dataframe we can see that the information in the species column is not particularly helpful - we can see that there are four character objects, but not what they contain or how many trees of which species we have (like with the iris dataset above). This is because R does currently not recognize that these characters represent **categorical data.**\n\n```{r}\nsummary(trees)\n```\n\nThe categorical data type in R is called a **factor**. You can easily convert a variable into a factor using the `factor()` function.\n\n```{r}\ntrees$species_factor <- factor(trees$species)\n\n# Now the species_factor column provides a more useful summary\nsummary(trees)\n```\n\nEach factor has **levels** (categories). You can use the `levels()` function or `str()` to inspect them.\n\n```{r}\nlevels(trees$species_factor)\nstr(trees)\n```\n\nInternally, a factor saves a number for each observation. The number represents the category; in this case, 1 represents \"Beech\" and 2 represents \"Fir\".\n\nWhen converting a variable into a factor, you can optionally use the arguments `levels` and `labels`. By specifying \"levels\", you can change the order of your factor levels (otherwise, levels will be ordered alphabetically by default). With \"labels\", you can change the name of each factor level that will displayed in the summary statistics. Specifying levels and labels becomes especially relevant when plotting categorical data (covered in lesson 5).\n\nFor example, if you want to change the order of your level so that \"Fir\" is the first level and change the display names to their scientific names, you would use this code:\n\n```{r}\n# You can split up function arguments into multiple lines for readability\ntrees$species_factor <- factor(\n  trees$species,\n  levels = c(\"Fir\", \"Beech\"),\n  labels = c(\"Abies alba\", \"Fagus sylvatica\")\n  )\nsummary(trees)\n```\n\n## Renaming Columns\n\nYou can use the `names()` function to access the column names of a data frame.\n\n```{r}\ncolnames(trees)\n```\n\nYou can also use this function to rename the columns using an assignment:\n\n```{r}\ncolnames(trees) <- c(\"dbh_cm\", \"height_m\", \"species_chr\", \"species_factor\")\nstr(trees)\n```\n"},"formats":{"html":{"identifier":{"display-name":"HTML","target-format":"html","base-format":"html"},"execute":{"fig-width":7,"fig-height":5,"fig-format":"retina","fig-dpi":96,"df-print":"default","error":false,"eval":true,"cache":null,"freeze":false,"echo":true,"output":true,"warning":true,"include":true,"keep-md":false,"keep-ipynb":false,"ipynb":null,"enabled":null,"daemon":null,"daemon-restart":false,"debug":false,"ipynb-filters":[],"ipynb-shell-interactivity":null,"plotly-connected":true,"engine":"knitr"},"render":{"keep-tex":false,"keep-typ":false,"keep-source":false,"keep-hidden":false,"prefer-html":false,"output-divs":true,"output-ext":"html","fig-align":"default","fig-pos":null,"fig-env":null,"code-fold":"none","code-overflow":"scroll","code-link":false,"code-line-numbers":false,"code-tools":false,"tbl-colwidths":"auto","merge-includes":true,"inline-includes":false,"preserve-yaml":false,"latex-auto-mk":true,"latex-auto-install":true,"latex-clean":true,"latex-min-runs":1,"latex-max-runs":10,"latex-makeindex":"makeindex","latex-makeindex-opts":[],"latex-tlmgr-opts":[],"latex-input-paths":[],"latex-output-dir":null,"link-external-icon":false,"link-external-newwindow":false,"self-contained-math":false,"format-resources":[],"notebook-links":true},"pandoc":{"standalone":true,"wrap":"none","default-image-extension":"png","to":"html","toc":true,"output-file":"03_Dataframes.html"},"language":{"toc-title-document":"Table of contents","toc-title-website":"On this page","related-formats-title":"Other Formats","related-notebooks-title":"Notebooks","source-notebooks-prefix":"Source","other-links-title":"Other Links","code-links-title":"Code Links","launch-dev-container-title":"Launch Dev Container","launch-binder-title":"Launch Binder","article-notebook-label":"Article Notebook","notebook-preview-download":"Download Notebook","notebook-preview-download-src":"Download Source","notebook-preview-back":"Back to Article","manuscript-meca-bundle":"MECA Bundle","section-title-abstract":"Abstract","section-title-appendices":"Appendices","section-title-footnotes":"Footnotes","section-title-references":"References","section-title-reuse":"Reuse","section-title-copyright":"Copyright","section-title-citation":"Citation","appendix-attribution-cite-as":"For attribution, please cite this work as:","appendix-attribution-bibtex":"BibTeX citation:","appendix-view-license":"View License","title-block-author-single":"Author","title-block-author-plural":"Authors","title-block-affiliation-single":"Affiliation","title-block-affiliation-plural":"Affiliations","title-block-published":"Published","title-block-modified":"Modified","title-block-keywords":"Keywords","callout-tip-title":"Tip","callout-note-title":"Note","callout-warning-title":"Warning","callout-important-title":"Important","callout-caution-title":"Caution","code-summary":"Code","code-tools-menu-caption":"Code","code-tools-show-all-code":"Show All Code","code-tools-hide-all-code":"Hide All Code","code-tools-view-source":"View Source","code-tools-source-code":"Source Code","tools-share":"Share","tools-download":"Download","code-line":"Line","code-lines":"Lines","copy-button-tooltip":"Copy to Clipboard","copy-button-tooltip-success":"Copied!","repo-action-links-edit":"Edit this page","repo-action-links-source":"View source","repo-action-links-issue":"Report an issue","back-to-top":"Back to top","search-no-results-text":"No results","search-matching-documents-text":"matching documents","search-copy-link-title":"Copy link to search","search-hide-matches-text":"Hide additional matches","search-more-match-text":"more match in this document","search-more-matches-text":"more matches in this document","search-clear-button-title":"Clear","search-text-placeholder":"","search-detached-cancel-button-title":"Cancel","search-submit-button-title":"Submit","search-label":"Search","toggle-section":"Toggle section","toggle-sidebar":"Toggle sidebar navigation","toggle-dark-mode":"Toggle dark mode","toggle-reader-mode":"Toggle reader mode","toggle-navigation":"Toggle navigation","crossref-fig-title":"Figure","crossref-tbl-title":"Table","crossref-lst-title":"Listing","crossref-thm-title":"Theorem","crossref-lem-title":"Lemma","crossref-cor-title":"Corollary","crossref-prp-title":"Proposition","crossref-cnj-title":"Conjecture","crossref-def-title":"Definition","crossref-exm-title":"Example","crossref-exr-title":"Exercise","crossref-ch-prefix":"Chapter","crossref-apx-prefix":"Appendix","crossref-sec-prefix":"Section","crossref-eq-prefix":"Equation","crossref-lof-title":"List of Figures","crossref-lot-title":"List of Tables","crossref-lol-title":"List of Listings","environment-proof-title":"Proof","environment-remark-title":"Remark","environment-solution-title":"Solution","listing-page-order-by":"Order By","listing-page-order-by-default":"Default","listing-page-order-by-date-asc":"Oldest","listing-page-order-by-date-desc":"Newest","listing-page-order-by-number-desc":"High to Low","listing-page-order-by-number-asc":"Low to High","listing-page-field-date":"Date","listing-page-field-title":"Title","listing-page-field-description":"Description","listing-page-field-author":"Author","listing-page-field-filename":"File Name","listing-page-field-filemodified":"Modified","listing-page-field-subtitle":"Subtitle","listing-page-field-readingtime":"Reading Time","listing-page-field-wordcount":"Word Count","listing-page-field-categories":"Categories","listing-page-minutes-compact":"{0} min","listing-page-category-all":"All","listing-page-no-matches":"No matching items","listing-page-words":"{0} words","listing-page-filter":"Filter","draft":"Draft"},"metadata":{"lang":"en","fig-responsive":true,"quarto-version":"1.9.38","engines":[{"path":"C:\\Program Files\\RStudio\\resources\\app\\bin\\quarto\\share\\extension-subtrees\\julia-engine\\_extensions\\julia-engine\\julia-engine.js"}],"theme":"cosmo","title":"Preperatory R Course - Topic 3","editor":"visual"},"extensions":{"book":{"multiFile":true}}}},"projectFormats":["html"]}